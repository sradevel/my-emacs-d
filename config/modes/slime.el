;; Set your lisp system and, optionally, some contribs
 (setq inferior-lisp-program "c:/Daten/03-Tools/ccl/wx86cl.exe")
(setq slime-contribs '(slime-fancy))
;; enable company-mode
 (slime-setup '(slime-company)) 
